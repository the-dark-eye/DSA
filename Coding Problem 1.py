"""
You are given an array of numbers (non-negative). Find a continuous subarray of the list which adds up to a given sum.

Inputs: List with positive integers, Target sum
Outputs: Subarray adding up to target sum
"""

# Try brute force

def subarray(lst: list, target: int):
    
        
